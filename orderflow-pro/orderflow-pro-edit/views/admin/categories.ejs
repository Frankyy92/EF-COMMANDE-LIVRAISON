<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Catégories - Admin - Orderflow</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <%- include('../partials/header') %>
  <div class="layout">
    <%- include('../partials/side_nav') %>
    <main class="main-content">
      <div class="container">
    <h1>Catégories</h1>
    <a href="/admin/categories/new" class="button">Nouvelle catégorie</a>
    <table>
      <thead>
        <tr>
          <th>Nom</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% categories.forEach(function(cat) { %>
          <tr>
            <td><%= cat.name %></td>
            <td>
              <a href="/admin/categories/<%= cat.id %>/edit" class="button">Éditer</a>
              <form action="/admin/categories/<%= cat.id %>/delete" method="post" style="display:inline-block" onsubmit="return confirm('Supprimer cette catégorie ?')">
                <button type="submit" class="button">Supprimer</button>
              </form>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
      </div>
    </main>
  </div>
</body>
</html>