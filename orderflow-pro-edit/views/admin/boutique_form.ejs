<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= boutique ? 'Modifier la boutique' : 'Nouvelle boutique' %> - Orderflow</title>
  <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
  <%- include('../partials/header') %>
  <div class="layout">
    <%- include('../partials/side_nav') %>
    <main class="main-content">
      <div class="container">
    <h1><%= boutique ? 'Modifier la boutique' : 'Nouvelle boutique' %></h1>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    <form action="<%= boutique ? '/admin/boutiques/' + boutique.id + '/edit' : '/admin/boutiques/new' %>" method="post">
      <div class="form-group">
        <label for="name">Nom</label>
        <input type="text" name="name" id="name" value="<%= boutique ? boutique.name : '' %>" required>
      </div>
      <button type="submit" class="button"><%= boutique ? 'Mettre à jour' : 'Créer' %></button>
    </form>
      </div>
    </main>
  </div>
</body>
</html>