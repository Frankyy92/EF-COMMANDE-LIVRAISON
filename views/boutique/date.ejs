
<div class="card"><h2><%= shop.name %> — Choisir une date</h2>
<form method="get" action="/boutique/<%= shop.id %>/<%= defaultDate %>">
  <label>Date</label><input class="input" type="date" name="d" value="<%= defaultDate %>" required>
  <div style="margin-top:8px"></div><button class="btn primary">Aller à la commande</button>
</form></div>
